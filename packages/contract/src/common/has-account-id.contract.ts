/**
 * A data contract for a record belonging to a top-level account.
 */
export interface HasAccountId {
    /**
     * Account ID of the account this record belongs to.
     */
    accountId: string;
}
